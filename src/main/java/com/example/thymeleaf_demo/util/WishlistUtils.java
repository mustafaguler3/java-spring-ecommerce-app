package com.example.thymeleaf_demo.util;

import com.example.thymeleaf_demo.domain.Wishlist;

import java.util.List;

/*public class WishlistUtils {
    public static boolean isProductInWishlist(List<Wishlist> wishlist,Long productId){
        if (wishlist == null){
            return false;
        }
        return wishlist.stream().anyMatch(item -> item.getProduct().getId().equals(productId));
    }
} */
